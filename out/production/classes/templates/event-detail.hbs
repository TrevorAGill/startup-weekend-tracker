{{#partial "content"}}


    <ul class="nav nav-tabs">
        <li role="presentation"><a href="/">Home</a></li>
        <li role="presentation"><a href="/events">All Events</a></li>
        <li role="presentation" class="active"><a href="/events">Event: {{foundEvent.name}}</a></li>
        <li role="presentation"><a href="/event/{{foundEvent.id}}/attendee/new">Add New Attendee</a></li>
    </ul>
    <div class="container">
        <!--begin main template-->
        <div class="page-header">
            <h1>{{foundEvent.name}}<small>{{foundEvent.description}}</small></h1><a href="/event/{{foundEvent.id}}/edit">Edit Event</a><a href="/event/{{foundEvent.id}}/delete"><br>Delete Event</a>
        </div>
        <div class="well">
            <h2>Attendee List</h2>
            {{#if attendees}}
                <table>
                    <tr>
                        <th width="40px">Edit/Delete</th>
                        <th>Name</th>
                        <th>Age</th>
                        <th>Company</th>
                        <th>Email</th>
                    </tr>
                    {{#each attendees}}
                        <tr>
                            <td width="40px"><a href="/event/{{foundEvent.id}}/attendee/{{id}}/edit"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>  <a href="/event/{{foundEvent.id}}/attendee/{{id}}/delete">  <span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a></td>
                            <td>{{name}}</a></td>
                            <td>{{age}}</td>
                            <td>{{company}}</td>
                            <td>{{email}}</td>
                        </tr>
                    {{/each}}
                </table>
            {{else}}
                <p>There are currently no attendees registered for this event.</p>
            {{/if}}
        </div>
    </div>

{{/partial}}
{{> layout.hbs}}